import React from "react";
import ConsultCard from "src/v2/components/reusable/cards/ConsultCard/ConsultCard";

export default function ProviderProfileCard({ profile }) {
  return <ConsultCard item={profile} />;
}
